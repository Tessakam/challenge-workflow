security:
  encoders:
    App\Entity\User:
      algorithm: auto

  # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
  providers:
    # used to reload user from session & other features (e.g. switch_user)
    app_user_provider:
      entity:
        class: App\Entity\User
        property: FirstName
  firewalls:
    dev:
      pattern: ^/(_(profiler|wdt)|css|images|js)/
      security: false
    main:
      anonymous: lazy
      provider: app_user_provider
      guard:
        authenticators:
          - App\Security\LoginFormAuthenticator
      logout:
        path: app_logout
        # where to redirect after logout
        target: app_login

      # activate different ways to authenticate
      # https://symfony.com/doc/current/security.html#firewalls-authentication

      # https://symfony.com/doc/current/security/impersonating_user.html
      # switch_user: true

  # Easy way to control access for large sections of your site
  # Note: Only the *first* access control that matches will be used
  access_control:
    - { path: ^/tickets, roles: ROLE_USER }
    - { path: ^/manager, roles: ROLE_MANAGER }
    - { path: ^/user, roles: ROLE_MANAGER }
    - { path: ^/customer, roles: ROLE_USER }
    - { path: ^/agent, roles: ROLE_AGENT_ONE }
    - { path: ^/agent, roles: ROLE_AGENT_TWO }

  role_hierarchy:
        ROLE_AGENT_ONE: [ ROLE_USER, ROLE_ALLOWED_TO_SWITCH ]
        ROLE_AGENT_TWO: [ ROLE_USER, ROLE_ALLOWED_TO_SWITCH ]
        ROLE_MANAGER: [ ROLE_AGENT_ONE,ROLE_AGENT_TWO, ROLE_ALLOWED_TO_SWITCH ]
